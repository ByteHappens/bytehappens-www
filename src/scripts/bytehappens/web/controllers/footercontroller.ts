import * as angular from "angular";

namespace ByteHappens.Web.Config {
  class FooterController {
    private static $inject: string[] = [];

    constructor() {}
  }

  angular.module("ByteHappens.Web").controller("FooterController", FooterController);
}
