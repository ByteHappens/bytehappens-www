import * as angular from "angular";

namespace ByteHappens.Web.Config {
  class HeaderController {
    private static $inject: string[] = [];

    constructor() {}
  }

  angular.module("ByteHappens.Web").controller("HeaderController", HeaderController);
}
